// Package memory provides in-memory stores for tests, examples, and prototypes.
//
// Store implements the current principal creation, identity linking, principal
// resolution, and API-token storage contracts. It is deterministic and
// concurrency-safe, but it is not a production persistence adapter.
package memory

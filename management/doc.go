// Package management provides reusable Go services for authkit setup flows.
//
// The package composes the lower-level authkit principal, role, identity-link,
// and API-token contracts. It is intended for application-owned setup paths
// such as CLIs, seed scripts, migrations, or admin handlers without providing
// an admin HTTP API itself.
package management

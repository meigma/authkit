// Package compose provides thin convenience wiring for common authkit setups.
//
// The package is a composition layer over the explicit authkit, apikey, oidc,
// and httpauth packages. It does not own storage, provider trust, policy
// setup, migrations, seed credentials, or management workflows.
package compose
